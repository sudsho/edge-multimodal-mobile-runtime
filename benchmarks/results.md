# Latency results

Not benchmarked in this repo. `src/bench/latency_probe.py` is a
desktop-only timer over torch / onnxruntime / coreml sessions on random
input. No on-device profiler runs, CSVs, or screenshots are checked in
here.

See [docs/latency_report.md](../docs/latency_report.md) for the shape of
what the bench script measures.
